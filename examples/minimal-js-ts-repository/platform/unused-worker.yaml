apiVersion: batch/v1
kind: Job
metadata:
  name: atlas-fixture-unused-worker
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: worker
          image: example.invalid/atlas-fixture-worker:latest
